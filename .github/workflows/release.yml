name: Release
on: [push]
jobs:
  release-package:
    runs-on: ubuntu-latest
    # name:
    steps:
      - uses: actions/checkout@v3
      - name: Setup Node.JS
        uses: actions/setup-node@v3
        with:
          node-version: '16'
      - run: yarn install
      - run: yarn test
      - run: ./changelog-bump.js
      # - run: OLD_VERSION="$(jq '.version' package.json | cut -d. -f2)"
      # - run: echo "OLD_VERSION- ${OLD_VERSION}"
      # - run: sed -i 's/^\*\* Upcoming' CHANGELOG.log
      - run: git add CHANGELOG.org
      - run: yarn version minor
      - run: yarn prepare
      # - run: yarn publish
      #   env:
      #     NODE_AUTH_TOKEN: ${{ secrets.YARN_TOKEN }}
      #- run: git push --tags
      # Github Actions catches this as an orphaned process, but let's be a good
      # citizen and clean up after ourselves.
      - run: yarn flow stop
      # TODO: This needs a token and permissions configured.
      # run: yarn publish
